import { PhasePlaceholder } from '../components/PhasePlaceholder'

export function NotFoundPage() {
  return (
    <PhasePlaceholder
      eyebrow="Error 404"
      title="Esta pagina no existe"
      description="Revisa la direccion o vuelve al marketplace de Rapicancha."
    />
  )
}
